<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:CrossPlatformDownloadManager.DesktopApp.ViewModels"
        xmlns:cc="using:CrossPlatformDownloadManager.DesktopApp.CustomControls"
        xmlns:controls="clr-namespace:CrossPlatformDownloadManager.DesktopApp.Views.UserControls.AddNewQueueWindowControls"
        mc:Ignorable="d"
        d:DesignWidth="800"
        d:DesignHeight="450"
        x:Class="CrossPlatformDownloadManager.DesktopApp.Views.AddNewQueueWindow"
        x:DataType="vm:AddNewQueueWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="CDM - Add New Category"
        Width="800"
        Height="600"
        MinWidth="800"
        MinHeight="600"
        MaxWidth="800"
        MaxHeight="600"
        ExtendClientAreaChromeHints="NoChrome"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaTitleBarHeightHint="0"
        CanResize="False">
    <Grid RowDefinitions="Auto, *, Auto"
          Margin="20">
        <!-- Tabs -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Spacing="10"
                    Margin="0, 0, 0, 15">
            <ToggleButton Classes="tabItemToggleButton"
                          IsChecked="{Binding ShowOptionsView}"
                          Command="{Binding ChangeViewCommand}"
                          CommandParameter="{Binding $self}"
                          Name="OptionsButton">
                <TextBlock Text="Options" />
            </ToggleButton>
            <ToggleButton Classes="tabItemToggleButton"
                          IsChecked="{Binding ShowFilesView}"
                          Command="{Binding ChangeViewCommand}"
                          CommandParameter="{Binding $self}"
                          Name="FilesButton">
                <TextBlock Text="Files" />
            </ToggleButton>
        </StackPanel>

        <!-- Options content -->
        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Hidden"
                      IsVisible="{Binding ShowOptionsView}">
            <StackPanel Orientation="Vertical"
                        Spacing="15">
                <!-- Must have 20px margin bottom. 15px spacing + 5px margin bottom -->
                <cc:CustomTextBox Padding="12, 10"
                                  Background="{DynamicResource SecondaryColor}"
                                  CornerRadius="{DynamicResource DefaultCornerRadius}"
                                  IconData="{DynamicResource Queue}"
                                  IconSize="16"
                                  IconColor="{DynamicResource ProgressBarGradientBrush}"
                                  Watermark="Queue Title"
                                  WatermarkForeground="{DynamicResource TertiaryColor}"
                                  Margin="0, 0, 0, 5"
                                  HorizontalAlignment="Stretch"
                                  OnHoverBorderColor="{DynamicResource ProgressBarGradientBrush}"
                                  OnFocusBorderColor="{DynamicResource ProgressBarGradientBrush}"
                                  OnErrorBorderColor="{DynamicResource DangerGradientBrush}"
                                  BorderThickness="1"
                                  BorderBrush="{DynamicResource SecondaryColor}"
                                  Text="{Binding QueueTitle}" />

                <!-- Start download on application startup section -->
                <Border CornerRadius="{DynamicResource DefaultCornerRadius}"
                        Padding="15"
                        Background="{DynamicResource SecondaryColor}">
                    <Grid ColumnDefinitions="*, Auto">
                        <TextBlock Grid.Column="0"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Center"
                                   TextAlignment="Left"
                                   Text="Start Download On Application Startup"
                                   FontWeight="Bold" />

                        <ToggleSwitch Grid.Column="1"
                                      OffContent="{x:Null}"
                                      OnContent="{x:Null}"
                                      Margin="0"
                                      Padding="0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Right"
                                      HorizontalContentAlignment="Right"
                                      VerticalContentAlignment="Center"
                                      Width="40"
                                      IsChecked="{Binding StartDownloadOnApplicationStartup}" />
                    </Grid>
                </Border>

                <!-- Start download schedule section -->
                <Border CornerRadius="{DynamicResource DefaultCornerRadius}"
                        Padding="15"
                        Background="{DynamicResource SecondaryColor}">
                    <StackPanel Orientation="Vertical"
                                Spacing="10">
                        <!-- Title -->
                        <Grid ColumnDefinitions="*, Auto">
                            <TextBlock Grid.Column="0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Center"
                                       TextAlignment="Left"
                                       Text="Start Download Schedule"
                                       FontWeight="Bold" />

                            <ToggleSwitch Grid.Column="1"
                                          OffContent="{x:Null}"
                                          OnContent="{x:Null}"
                                          Margin="0"
                                          Padding="0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Right"
                                          HorizontalContentAlignment="Right"
                                          VerticalContentAlignment="Center"
                                          Width="40"
                                          IsChecked="{Binding StartDownloadScheduleEnabled}" />
                        </Grid>

                        <!-- Start download schedule -->
                        <StackPanel Orientation="Horizontal"
                                    Spacing="10"
                                    HorizontalAlignment="Left">
                            <cc:CustomNumericUpDown Padding="12, 10"
                                                    Background="{DynamicResource PrimaryColor}"
                                                    CornerRadius="{DynamicResource DefaultCornerRadius}"
                                                    Watermark="Hour"
                                                    WatermarkForeground="{DynamicResource TertiaryColor}"
                                                    HorizontalAlignment="Stretch"
                                                    BorderThickness="0"
                                                    Value="{Binding StartDownloadHour}"
                                                    IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                                    VerticalAlignment="Center"
                                                    MinWidth="80"
                                                    ShowButtonSpinner="False"
                                                    Increment="1"
                                                    Minimum="1"
                                                    Maximum="12"
                                                    ClipValueToMinMax="True"
                                                    FormatString="00" />

                            <TextBlock Text=":"
                                       VerticalAlignment="Center" />

                            <cc:CustomNumericUpDown Padding="12, 10"
                                                    Background="{DynamicResource PrimaryColor}"
                                                    CornerRadius="{DynamicResource DefaultCornerRadius}"
                                                    Watermark="Minute"
                                                    WatermarkForeground="{DynamicResource TertiaryColor}"
                                                    HorizontalAlignment="Stretch"
                                                    BorderThickness="0"
                                                    Value="{Binding StartDownloadMinute}"
                                                    IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                                    VerticalAlignment="Center"
                                                    MinWidth="80"
                                                    ShowButtonSpinner="False"
                                                    Increment="1"
                                                    Minimum="0"
                                                    Maximum="59"
                                                    ClipValueToMinMax="True"
                                                    FormatString="00" />

                            <cc:CustomComboBox PlaceholderText="AM / PM"
                                               MinWidth="80"
                                               Background="{DynamicResource PrimaryColor}"
                                               ToggleIconColor="{DynamicResource TertiaryColor}"
                                               OnHoverToggleIconColor="{DynamicResource TextColor}"
                                               ItemsSource="{Binding TimesOfDay}"
                                               SelectedItem="{Binding SelectedStartTimeOfDay}"
                                               IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                               VerticalAlignment="Center"
                                               Margin="10, 0, 0, 0">
                                <cc:CustomComboBox.ItemTemplate>
                                    <DataTemplate DataType="x:String">
                                        <TextBlock Text="{Binding}" />
                                    </DataTemplate>
                                </cc:CustomComboBox.ItemTemplate>
                            </cc:CustomComboBox>
                        </StackPanel>

                        <!-- Start download date -->
                        <StackPanel Orientation="Horizontal"
                                    Spacing="10"
                                    HorizontalAlignment="Left"
                                    Margin="0, 5, 0, 0">
                            <RadioButton GroupName="StartDownloadDate"
                                         IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                         Classes="startDownloadDate"
                                         Command="{Binding ChangeStartDownloadDateCommand}"
                                         CommandParameter="Today"
                                         IsChecked="{Binding !IsDailyDownload}">
                                Today
                            </RadioButton>

                            <RadioButton GroupName="StartDownloadDate"
                                         IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                         Classes="startDownloadDate"
                                         Command="{Binding ChangeStartDownloadDateCommand}"
                                         CommandParameter="Daily"
                                         IsChecked="{Binding IsDailyDownload}">
                                Daily
                            </RadioButton>
                        </StackPanel>

                        <!-- Days of week -->
                        <StackPanel Orientation="Horizontal"
                                    Spacing="5">
                            <CheckBox Classes="dayOfWeek"
                                      IsVisible="{Binding IsDailyDownload}"
                                      IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                      IsChecked="{Binding DaysOfWeek.Saturday}">
                                Saturday
                            </CheckBox>

                            <CheckBox Classes="dayOfWeek"
                                      IsVisible="{Binding IsDailyDownload}"
                                      IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                      IsChecked="{Binding DaysOfWeek.Sunday}">
                                Sunday
                            </CheckBox>

                            <CheckBox Classes="dayOfWeek"
                                      IsVisible="{Binding IsDailyDownload}"
                                      IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                      IsChecked="{Binding DaysOfWeek.Monday}">
                                Monday
                            </CheckBox>

                            <CheckBox Classes="dayOfWeek"
                                      IsVisible="{Binding IsDailyDownload}"
                                      IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                      IsChecked="{Binding DaysOfWeek.Tuesday}">
                                Tuesday
                            </CheckBox>

                            <CheckBox Classes="dayOfWeek"
                                      IsVisible="{Binding IsDailyDownload}"
                                      IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                      IsChecked="{Binding DaysOfWeek.Wednesday}">
                                Wednesday
                            </CheckBox>

                            <CheckBox Classes="dayOfWeek"
                                      IsVisible="{Binding IsDailyDownload}"
                                      IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                      IsChecked="{Binding DaysOfWeek.Thursday}">
                                Thursday
                            </CheckBox>

                            <CheckBox Classes="dayOfWeek"
                                      IsVisible="{Binding IsDailyDownload}"
                                      IsEnabled="{Binding StartDownloadScheduleEnabled}"
                                      IsChecked="{Binding DaysOfWeek.Friday}">
                                Friday
                            </CheckBox>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Stop download schedule section -->
                <Border CornerRadius="{DynamicResource DefaultCornerRadius}"
                        Padding="15"
                        Background="{DynamicResource SecondaryColor}">
                    <StackPanel Orientation="Vertical"
                                Spacing="10">
                        <!-- Title -->
                        <Grid ColumnDefinitions="*, Auto">
                            <TextBlock Grid.Column="0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Center"
                                       TextAlignment="Left"
                                       Text="Stop Download Schedule"
                                       FontWeight="Bold" />

                            <ToggleSwitch Grid.Column="1"
                                          OffContent="{x:Null}"
                                          OnContent="{x:Null}"
                                          Margin="0"
                                          Padding="0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Right"
                                          HorizontalContentAlignment="Right"
                                          VerticalContentAlignment="Center"
                                          Width="40"
                                          IsChecked="{Binding StopDownloadScheduleEnabled}" />
                        </Grid>

                        <!-- Stop download schedule -->
                        <StackPanel Orientation="Horizontal"
                                    Spacing="10"
                                    HorizontalAlignment="Left">
                            <cc:CustomNumericUpDown Padding="12, 10"
                                                    Background="{DynamicResource PrimaryColor}"
                                                    CornerRadius="{DynamicResource DefaultCornerRadius}"
                                                    Watermark="Hour"
                                                    WatermarkForeground="{DynamicResource TertiaryColor}"
                                                    HorizontalAlignment="Stretch"
                                                    BorderThickness="0"
                                                    Value="{Binding StopDownloadHour}"
                                                    IsEnabled="{Binding StopDownloadScheduleEnabled}"
                                                    VerticalAlignment="Center"
                                                    MinWidth="80"
                                                    ShowButtonSpinner="False"
                                                    Increment="1"
                                                    Minimum="1"
                                                    Maximum="12"
                                                    ClipValueToMinMax="True"
                                                    FormatString="00" />

                            <TextBlock Text=":"
                                       VerticalAlignment="Center" />

                            <cc:CustomNumericUpDown Padding="12, 10"
                                                    Background="{DynamicResource PrimaryColor}"
                                                    CornerRadius="{DynamicResource DefaultCornerRadius}"
                                                    Watermark="Minute"
                                                    WatermarkForeground="{DynamicResource TertiaryColor}"
                                                    HorizontalAlignment="Stretch"
                                                    BorderThickness="0"
                                                    Value="{Binding StopDownloadMinute}"
                                                    IsEnabled="{Binding StopDownloadScheduleEnabled}"
                                                    VerticalAlignment="Center"
                                                    MinWidth="80"
                                                    ShowButtonSpinner="False"
                                                    Increment="1"
                                                    Minimum="0"
                                                    Maximum="59"
                                                    ClipValueToMinMax="True"
                                                    FormatString="00" />

                            <cc:CustomComboBox PlaceholderText="AM / PM"
                                               MinWidth="80"
                                               Background="{DynamicResource PrimaryColor}"
                                               ToggleIconColor="{DynamicResource TertiaryColor}"
                                               OnHoverToggleIconColor="{DynamicResource TextColor}"
                                               ItemsSource="{Binding TimesOfDay}"
                                               SelectedItem="{Binding SelectedStopTimeOfDay}"
                                               IsEnabled="{Binding StopDownloadScheduleEnabled}"
                                               VerticalAlignment="Center"
                                               Margin="10, 0, 0, 0">
                                <cc:CustomComboBox.ItemTemplate>
                                    <DataTemplate DataType="x:String">
                                        <TextBlock Text="{Binding}" />
                                    </DataTemplate>
                                </cc:CustomComboBox.ItemTemplate>
                            </cc:CustomComboBox>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Retry on download failed section -->
                <Border CornerRadius="{DynamicResource DefaultCornerRadius}"
                        Padding="15"
                        Background="{DynamicResource SecondaryColor}">
                    <StackPanel Orientation="Vertical"
                                Spacing="10">
                        <!-- Title -->
                        <Grid ColumnDefinitions="*, Auto">
                            <TextBlock Grid.Column="0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Center"
                                       TextAlignment="Left"
                                       Text="Retry On Download Failed"
                                       FontWeight="Bold" />

                            <ToggleSwitch Grid.Column="1"
                                          OffContent="{x:Null}"
                                          OnContent="{x:Null}"
                                          Margin="0"
                                          Padding="0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Right"
                                          HorizontalContentAlignment="Right"
                                          VerticalContentAlignment="Center"
                                          Width="40"
                                          IsChecked="{Binding RetryOnDownloadFailed}" />
                        </Grid>

                        <cc:CustomNumericUpDown BorderThickness="0"
                                                Background="{DynamicResource PrimaryColor}"
                                                CornerRadius="{DynamicResource DefaultCornerRadius}"
                                                Watermark="Number Of Retries"
                                                WatermarkForeground="{DynamicResource TertiaryColor}"
                                                Padding="12, 10"
                                                HorizontalAlignment="Stretch"
                                                Increment="1"
                                                Minimum="0"
                                                Maximum="100"
                                                Value="{Binding NumberOfRetries}"
                                                ShowButtonSpinner="False"
                                                ClipValueToMinMax="True"
                                                IsEnabled="{Binding RetryOnDownloadFailed}" />
                    </StackPanel>
                </Border>

                <!-- Show alarm when done section -->
                <Border CornerRadius="{DynamicResource DefaultCornerRadius}"
                        Padding="15"
                        Background="{DynamicResource SecondaryColor}">
                    <Grid ColumnDefinitions="*, Auto">
                        <TextBlock Grid.Column="0"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Center"
                                   TextAlignment="Left"
                                   Text="Show Alarm When Done"
                                   FontWeight="Bold" />

                        <ToggleSwitch Grid.Column="1"
                                      OffContent="{x:Null}"
                                      OnContent="{x:Null}"
                                      Margin="0"
                                      Padding="0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Right"
                                      HorizontalContentAlignment="Right"
                                      VerticalContentAlignment="Center"
                                      Width="40"
                                      IsChecked="{Binding ShowAlarmWhenDone}" />
                    </Grid>
                </Border>

                <!-- Exit program when done section -->
                <Border CornerRadius="{DynamicResource DefaultCornerRadius}"
                        Padding="15"
                        Background="{DynamicResource SecondaryColor}">
                    <Grid ColumnDefinitions="*, Auto">
                        <TextBlock Grid.Column="0"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Center"
                                   TextAlignment="Left"
                                   Text="Exit Program When Done"
                                   FontWeight="Bold" />

                        <ToggleSwitch Grid.Column="1"
                                      OffContent="{x:Null}"
                                      OnContent="{x:Null}"
                                      Margin="0"
                                      Padding="0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Right"
                                      HorizontalContentAlignment="Right"
                                      VerticalContentAlignment="Center"
                                      Width="40"
                                      IsChecked="{Binding ExitProgramWhenDone}" />
                    </Grid>
                </Border>

                <!-- Turn off computer when done section -->
                <Border CornerRadius="{DynamicResource DefaultCornerRadius}"
                        Padding="15"
                        Background="{DynamicResource SecondaryColor}">
                    <StackPanel Orientation="Vertical"
                                Spacing="10">
                        <Grid ColumnDefinitions="*, Auto">
                            <TextBlock Grid.Column="0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Center"
                                       TextAlignment="Left"
                                       Text="Turn Off Computer When Done"
                                       FontWeight="Bold" />

                            <ToggleSwitch Name="TurnOffComputerWhenDoneToggleSwitch"
                                          Grid.Column="1"
                                          OffContent="{x:Null}"
                                          OnContent="{x:Null}"
                                          Margin="0"
                                          Padding="0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Right"
                                          HorizontalContentAlignment="Right"
                                          VerticalContentAlignment="Center"
                                          Width="40"
                                          IsChecked="{Binding TurnOffComputerWhenDone}" />
                        </Grid>

                        <cc:CustomComboBox PlaceholderText="AM / PM"
                                           MinWidth="80"
                                           Background="{DynamicResource PrimaryColor}"
                                           ToggleIconColor="{DynamicResource TertiaryColor}"
                                           OnHoverToggleIconColor="{DynamicResource TextColor}"
                                           ItemsSource="{Binding TurnOffComputerModes}"
                                           SelectedItem="{Binding SelectedTurnOffComputerMode}"
                                           IsEnabled="{Binding #TurnOffComputerWhenDoneToggleSwitch.IsChecked}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right"
                                           Margin="10, 0, 0, 0">
                            <cc:CustomComboBox.ItemTemplate>
                                <DataTemplate DataType="x:String">
                                    <TextBlock Text="{Binding}" />
                                </DataTemplate>
                            </cc:CustomComboBox.ItemTemplate>
                        </cc:CustomComboBox>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Files content -->
        <controls:FilesView Grid.Row="1"
                      IsVisible="{Binding ShowFilesView}"
                      FilesItemsSource="{Binding DownloadFiles}"
                      DownloadQueueId="{Binding DownloadQueueId}"
                      DownloadQueueListPriorityChanged="FilesView_OnDownloadQueueListPriorityChanged"/>

        <!-- Action buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Spacing="10"
                    HorizontalAlignment="Right"
                    Margin="0, 15, 0, 0">
            <Button Classes="button success"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding $parent[Window]}">
                Save
            </Button>

            <Button Name="CancelButton"
                    Classes="button danger"
                    Click="CancelButton_OnClick">
                Cancel
            </Button>
        </StackPanel>
    </Grid>
</Window>